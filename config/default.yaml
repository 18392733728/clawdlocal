agent:
  name: "ClawdLocal"
  version: "0.1.0"
  description: "Lightweight local AI agent framework"
  workspace: "./workspace"

server:
  host: "localhost"
  port: 8080

plugins:
  enabled: true
  paths:
    - "./plugins"
  auto_reload: false

logging:
  level: "info"
  format: "text"
  output: "stdout"